<Page
    x:Class="InstallVibe.Views.Admin.AdminEditorPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <CommandBar Grid.Row="0">
            <AppBarButton
                Icon="Refresh"
                Label="Refresh"
                Command="{x:Bind ViewModel.LoadGuidesCommand}"/>
        </CommandBar>

        <ListView
            Grid.Row="1"
            ItemsSource="{x:Bind ViewModel.Guides, Mode=OneWay}"
            SelectedItem="{x:Bind ViewModel.SelectedGuide, Mode=TwoWay}"
            Margin="24">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Padding="12" Spacing="8">
                        <TextBlock
                            Text="{Binding Title}"
                            Style="{ThemeResource SubtitleTextBlockStyle}"/>
                        <TextBlock
                            Text="{Binding GuideId}"
                            Style="{ThemeResource CaptionTextBlockStyle}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <ProgressRing
            Grid.Row="1"
            IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}"
            Visibility="{x:Bind ViewModel.IsLoading, Mode=OneWay}"/>
    </Grid>
</Page>
